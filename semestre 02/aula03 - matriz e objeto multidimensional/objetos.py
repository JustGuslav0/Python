# Uma matriz é uma lista de listas com mesma quantidade de celulas dentro de cada uma
m = [
    [1,2,3],
    [4,5,6],
    [7,8,9]
]

# Primeiro é a linha e depois a coluna
# m[1][0]

# Para alterar o valor de alguma celula
# m[1][0] = 3

# Para consultar a qntd de colunas
# tam_cols = len(m)

# Para consultar a qntd de linhas
# tam_lins = len(m[0])

